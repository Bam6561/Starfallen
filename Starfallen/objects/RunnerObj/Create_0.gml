/// @description Path
// You can write your code in this editor

guess = 0;
//guess = irandom_range(0,1);
guess = choose(0,2,4,6)
if(guess == 0 || guess = 4)
{
path_start(RunnerPath, 6,path_action_stop, true);
}


if(guess == 2 || guess == 6 )
{
path_start(RunnerPath2, 6,path_action_stop, true);
}
